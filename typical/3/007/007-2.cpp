#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin >> n;
	int a[n];
	for (int i = 0; i < n; i++;) cin >> a[i];
	int q;
	cin >> q;
	for (int i = 0; i < n; i++) cin >> b[i];

	sort(a, a + n);
	
}
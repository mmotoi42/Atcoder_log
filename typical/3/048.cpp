#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,k;
	cin >> n >> k;
	pair<int, int> ab[n];
	for (int i = 0; i < n; i++) cin >> ab[i].first >> ab[i].second;

	sort(ab, ab + n, greater<int>());

	


}
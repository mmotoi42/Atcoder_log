#include <iostream>
using namespace std;
int main()
{
	int n, m;
	cin >> n >> m;
	int k[m];
	int s[m][m];
	for (int i = 0; i < m; i++) {
		cin >> k[i];
		for (int j = 0; j < k[i]; j++) {
			cin >> s[i][j];
		}
	}
	int p[m];
	for (int i = 0; i < m; i++) cin >> p[i];

	for (int i = 0; i < m; i++) {
		
	}
}
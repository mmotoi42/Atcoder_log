#include <bits/stdc++.h>
using namespace std;

typedef struct s_Person
{
	int a, b, c, d, e;
} Person;


int main()
{
	int n;
	cin >> n;
	Person p[n];
	for (int i = 0; i < n; i++)
		cin >> p[i].a >> p[i].b >> p[i].c >> p[i].d >> p[i].e;
	
	Person team;
	team.a = 0; team.b = 0; team.c = 0; team.d = 0; team.e = 0;
	int maxp = 0;
	for (int i = 0; i < n; i++) {
		
	}
	
}